[build]
  publish = ".next"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  SITE_NAME = "Kapil Iyer Portfolio"

# Redirects for single-page apps
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
